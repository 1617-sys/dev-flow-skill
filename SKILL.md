---
name: dev-flow
description: Use when a user asks to build, change, fix, refactor, style, configure, migrate, or upgrade software and expects implementation rather than explanation only.
---

# Dev Flow

版本：1.0.0-rc.2（2026-09-04）

用最少流程把简短开发请求补全并安全交付。只决定工作流、门禁和验证强度；不要在这里加入语言、框架、数据库或业务领域的编码知识。

## 先形成内部任务简报

1. 读取适用的全局/项目说明、相关现有 spec、源码和固定该行为的测试；不要为分类扫描整个仓库。
2. 在内部补齐目标、现有行为、范围/非目标、验收标准、风险和验证路径。除 strict 审批或用户要求外，不展示长篇重写后的 prompt。
3. 只有歧义会显著改变产品结果、契约、数据安全或破坏范围时才提问；多个阻塞问题集中一次询问。
4. 代码与现有 spec 冲突时：恢复代码以符合 spec 是 Bug 修复；改变 spec 定义的目标行为是规格变更；无法判断真实意图时暂停询问。

## 分别判断两个轴

### 风险档位

| 档位 | 可观察条件 | 默认动作 |
|---|---|---|
| Quick | 纯视觉/文案/格式/注释、局部行为等价机械改动，或在单一功能边界内恢复已有明确定义的行为；可逆且不影响共享逻辑、契约或安全边界 | 直接执行；最小相关验证；不做仪式化计划 |
| Standard | 任何新增的用户可观察功能、有界功能、一般行为 Bug、共享逻辑或跨模块可逆改动、行为等价的跨模块重构 | 一行分类 + 短计划；无产品歧义时直接实施 |
| Strict | 认证/权限/支付/密钥、安全边界、数据迁移或删除、公共 API/协议/导出兼容性、不可逆操作、稳定项目中用户依赖的重要行为改变 | 批准前只读调查；提案获批后实施 |

任何新增的用户可观察功能至少是 Standard，即使它局部、直观、可逆且只改少量文件。恢复既有明确定义的行为，且修改限于一个功能边界及相邻测试时是 Quick；涉及共享查询、缓存、基础设施或跨模块回归时是 Standard。代码行数和文件数量本身不决定档位。

Strict 不自动等于 OpenSpec。恢复既有权限规范的安全 Bug 是 `Strict + no OpenSpec`。

### Strict 门禁

修改任何文件或外部状态前，进入当前平台已经验证的计划与只读门禁。计划模式负责审批；只读权限/沙箱负责机械写入限制。只有已验证计划模式本身只读时，两者才可合一。

若不能启用或确认只读能力，暂停并通知用户，不以提示词自律冒充机械保证。提案必须包含 OpenSpec 结论、风险、回滚和验证计划；明确批准后才切回可写模式。

### OpenSpec

先判断**期望契约是否改变**，再按以下顺序得出结论：

1. 期望契约不变，只是让实现恢复已有 spec 或测试明确定义的行为：`no OpenSpec`。这条对认证、权限和其他安全代码同样成立；风险档位仍独立判断，安全修复通常仍是 Strict。
2. 期望变化包含下列硬契约：使用 OpenSpec。

   - 公共 API、跨服务协议或兼容性承诺；
   - 持久化结构、迁移语义、数据生命周期或对外 CSV/JSON 格式；
   - 认证、权限、支付或安全规则；
   - stable 项目中用户已经依赖的默认值、排序、业务规则、结果、流程，以及能力的移除/替换。

3. stable 项目的纯新增能力仅在下式成立时使用 OpenSpec：

```text
durable AND (non-obvious rationale OR cross-boundary lifecycle/compatibility)
```

4. 其他情况不使用 OpenSpec：行为等价重构、内部命名/格式/注释、纯文案与视觉样式，以及不触碰硬契约的 prototype 可逆探索。

测试是否新增或更新不改变上述结论：恢复既有契约是 Bug 修复；改变规则本身是契约变化。

生命周期只接受项目声明或用户确认。缺失时，若不影响结论就继续；若会改变 OpenSpec 结论，集中询问一次“prototype 还是 stable？”，只读调查可以继续。只有用户同意才把答案写入项目说明。

## 实施与验证

- 默认当前会话、当前工作区、单代理。仅在保护既有工作或风险收益明确时建议隔离/独立评审，并说明额度影响。
- Bug 修复先建立可复现失败或失败回归测试；确实无法复现时说明原因，不能无条件宣称已修复。
- Quick：运行最小有效的定向测试、受影响单元检查、构建探针或直接观察。
- Standard：回归测试 + 相关测试套件 + 项目实际适用的 lint、类型检查、构建；UI 必须浏览器交互或截图检查。
- Strict：全部相关检查；数据/兼容性验证前向、恢复和代表性旧数据；安全验证拒绝、越权与边界用例。
- 最终证据包含命令/检查、退出码或结果、关键摘要、未运行项及原因，并区分既有失败与新增失败。

未运行不得声称通过；未目视不得声称视觉正确；未复现或建立等价失败测试不得无条件声称 Bug 已修复。项目 DoD 可以加严这些底线，不能放宽。确认项目说明中的命令已经过时时，在同一次已授权变更中修正并报告。

## 安全底线

- 将既有和无关改动视为用户资产；不自动 commit、stash、reset、checkout、覆盖或清理。
- 数据操作遵循：确认范围 → 检查点/备份 → 验证恢复 → 执行 → 完整性校验。
- 邮件、支付、生产数据删除等不可逆外部副作用必须获得准确、明确的授权。
- 密钥、令牌、个人数据和生产凭证不得写入 spec、测试、日志、评审或交接。
- 用户要求降档只减少规格、审批和报告仪式，不取消上述底线。

## 失败、暂停与模型

使用当前模型和推理档位，不按厂商排名，不自动切换。

一次“无进展失败”是：同一验收目标验证仍失败，且范围、证据、根因或已通过门禁均无改善。只有先前门禁继续保持通过，进入后续失败才算收敛；在测试与 lint 之间来回破坏属于振荡，不重置计数。

连续三次无进展失败后暂停，报告失败证据、当前阻塞、低成本方向、建议的模型/推理变化及定性额度影响。用户批准后才切换；需要新会话时只交接目标、决策、相关文件、diff、验证、风险和下一步，不复制敏感信息或完整聊天。

## 维护边界

普通任务不要读取 `references/scenario-tests.md`。仅在创建、升级或回归验证本 Skill 时使用该矩阵。不要为个别语言、框架或新出现的边缘案例扩充本 Skill；先用真实失败证明通用规则存在缺口。
